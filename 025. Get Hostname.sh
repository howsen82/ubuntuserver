# Getting current host name
hostname

# Change current host name
sudo hostnamectl set-hostname dev.mynetwork.org
# Edit hosts name if not updated on /etc/hosts
# 127.0.1.1 dev.mynetwork.org dev

# View host name entry
cat /etc/hostname

# View hosts entrries
cat /etc/hosts
